<nav class="navbar sticky-top navbar-toggleable-md navbar-inverse bg-primary">
    <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="sr-only">Toggle navigation</span>
        <span class="navbar-toggler-icon"></span>
    </button>
    <a class="navbar-brand" href="/">TinyApp</a>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="/urls/new">Make a new TinyURL</a>
            </li>
        </ul>


        <% if (user) { %>
        <span class="navbar-text nav-item"><%= user.email %> is logged in</span>
        <form class="form-inline" action="/logout" method="post">
            <button type="submit" class="btn btn-outline-warning">Logout</button>
        </form>
        <% } else {%>
            <button type="button" class="btn btn-warning"><a href="/login">Login</a></button>
            <button type="button" class="btn btn-success"><a href="/register">Register</a></button>
        <% } %>
    </div>
</nav>
